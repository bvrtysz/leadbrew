{
  "name": "leadbrew",
  "version": "1.0.0",
  "description": "LeadBrew - Cay ve Kahve Musteri Bulma Otomasyonu",
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "start": "cd server && node index.js",
    "dev:server": "cd server && npm install && node index.js",
    "dev:client": "cd client && npm install && npm run dev"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
